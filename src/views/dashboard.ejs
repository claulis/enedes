<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema ENEDES - Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <script>
        function confirmDelete(message, url) {
            if (confirm(message)) {
                window.location.href = url;
            }
        }
    </script>
</head>
<body class="bg-white">
    <header class="bg-primary text-white text-center py-3 shadow-sm">
        <div class="container">
            <h1 class="display-5 fw-bold">Sistema ENEDES</h1>
            <nav class="mt-2 d-flex justify-content-center flex-wrap gap-2">
                <a href="/export" class="btn btn-outline-light btn-sm">📥 Exportar</a>
                <a href="/logout" class="btn btn-outline-light btn-sm">🚪 Sair</a>
            </nav>
        </div>
    </header>
    <main class="container-fluid min-vh-100 d-flex align-items-center justify-content-center">
        <div class="p-4 rounded-3 shadow bg-white border border-primary border-opacity-50 w-100" style="max-width: 1200px;">
            <div class="card p-4 mb-4 shadow-sm bg-white border border-primary border-opacity-50">
                <h3 class="mb-4 fw-bold text-dark">Filtros</h3>
                <form action="/dashboard" method="GET" class="row g-3 align-items-end">
                    <div class="col-12 col-md-4">
                        <label for="section" class="form-label text-dark">Seção</label>
                        <select name="section" id="section" class="form-select form-select-sm">
                            <option value="">Selecione uma seção...</option>
                            <% sections.forEach(section => { %>
                                <option value="<%= section %>" <%= filters.section === section ? 'selected' : '' %>><%= section %></option>
                            <% }); %>
                        </select>
                    </div>
                    <div class="col-12 col-md-3">
                        <label for="status" class="form-label text-dark">Status</label>
                        <select name="status" id="status" class="form-select form-select-sm">
                            <option value="" <%= !filters.status ? 'selected' : '' %>>Todos</option>
                            <option value="em_andamento" <%= filters.status === 'em_andamento' ? 'selected' : '' %>>Em Andamento</option>
                            <option value="concluida" <%= filters.status === 'concluida' ? 'selected' : '' %>>Concluída</option>
                            <option value="pendente" <%= filters.status === 'pendente' ? 'selected' : '' %>>Pendente</option>
                            <option value="alerta" <%= filters.status === 'alerta' ? 'selected' : '' %>>Alerta</option>
                        </select>
                    </div>
                    <div class="col-12 col-md-3">
                        <label for="search" class="form-label text-dark">Buscar</label>
                        <input type="text" name="search" id="search" class="form-control form-control-sm" placeholder="🔍 Buscar por tarefa ou responsável" value="<%= filters.search || '' %>">
                    </div>
                    <div class="col-12 col-md-2">
                        <button type="submit" class="btn btn-primary btn-sm w-100">Filtrar</button>
                    </div>
                </form>
            </div>
            <div class="actions-section">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="fw-bold text-dark">Ações</h2>
                    <a href="/action/new" class="btn btn-primary btn-sm">Nova Ação</a>
                </div>
                <% if (actions.length === 0) { %>
                    <div class="alert alert-info text-center" role="alert">
                        Nenhuma ação encontrada.
                    </div>
                <% } else { %>
                    <div class="row">
                        <% actions.forEach(action => { %>
                            <div class="col-12 col-md-6 col-lg-4 mb-4">
                                <div class="card h-100 shadow-sm bg-white border border-primary border-opacity-50">
                                    <div class="card-body">
                                        <h3 class="card-title fs-5 fw-bold text-dark"><%= action.task %></h3>
                                        <p class="card-text"><strong>Responsável:</strong> <%= action.responsible %></p>
                                        <p class="card-text"><strong>Seção:</strong> <%= action.section %></p>
                                        <p class="card-text"><strong>Status:</strong> <span class="badge <%= action.status === 'concluida' ? 'bg-primary' : action.status === 'em_andamento' ? 'bg-info' : action.status === 'alerta' ? 'bg-warning' : 'bg-danger' %>"><%= action.status %></span></p>
                                        <p class="card-text"><strong>Deadline:</strong> <%= action.deadline.toISOString().split('T')[0] %></p>
                                        <h4 class="mt-3 fs-6 text-dark">Tarefas:</h4>
                                        <ul class="list-group list-group-flush">
                                            <% action.tasks.forEach(task => { %>
                                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                                    <span><%= task.description %> - <em><%= task.completed ? 'Concluída' : 'Pendente' %></em></span>
                                                    <button class="btn btn-sm btn-outline-danger" onclick="confirmDelete('Tem certeza que deseja excluir esta tarefa?', '/task/delete/<%= task.id %>')">Excluir</button>
                                                </li>
                                            <% }); %>
                                        </ul>
                                        <div class="mt-3 d-flex gap-2">
                                            <a href="/action/edit/<%= action.id %>" class="btn btn-warning btn-sm">Editar</a>
                                            <button class="btn btn-danger btn-sm" onclick="confirmDelete('Tem certeza que deseja excluir esta ação?', '/action/delete/<%= action.id %>')">Excluir</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
</body>
</html>