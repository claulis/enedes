<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema ENEDES - Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow-lg p-4" style="max-width: 500px; width: 100%;">
            <!-- Logo ENEDES -->
            <div class="text-center mb-4">
                <svg width="400" height="120" viewBox="0 0 800 240" xmlns="http://www.w3.org/2000/svg">
                    <defs>
                        <linearGradient id="greenGradient" x1="0%" y1="0%" x2="100%" y2="0%">
                            <stop offset="0%" style="stop-color:#4CAF50;stop-opacity:1" />
                            <stop offset="100%" style="stop-color:#2E7D32;stop-opacity:1" />
                        </linearGradient>
                        <linearGradient id="orangeGradient" x1="0%" y1="0%" x2="100%" y2="0%">
                            <stop offset="0%" style="stop-color:#FF9800;stop-opacity:1" />
                            <stop offset="100%" style="stop-color:#F57C00;stop-opacity:1" />
                        </linearGradient>
                    </defs>
                    <rect x="20" y="40" width="80" height="120" rx="15" fill="url(#greenGradient)"/>
                    <rect x="30" y="60" width="50" height="15" fill="white"/>
                    <rect x="30" y="90" width="40" height="15" fill="white"/>
                    <rect x="30" y="120" width="50" height="15" fill="white"/>
                    <rect x="60" y="75" width="30" height="20" fill="url(#orangeGradient)"/>
                    <text x="120" y="100" font-family="Arial, sans-serif" font-size="48" font-weight="bold" fill="#4A6FA5">enedes</text>
                    <rect x="120" y="120" width="300" height="3" fill="#FF9800"/>
                    <text x="120" y="145" font-family="Arial, sans-serif" font-size="16" fill="#4A6FA5">Escola de Negócios e Desenvolvimento Social</text>
                    <text x="120" y="165" font-family="Arial, sans-serif" font-size="16" fill="#4A6FA5">do Instituto Federal de Brasília</text>
                </svg>
            </div>
            
            <h2 class="text-center mb-4">Sistema ENEDES - Gestão Completa</h2>
            <form id="loginForm" action="/login" method="POST">
                <div class="mb-3">
                    <label for="userSelect" class="form-label fw-bold">Selecione o usuário:</label>
                    <select id="userSelect" name="username" class="form-select" required>
                        <option value="">Escolha um usuário...</option>
                        <% users.forEach(user => { %>
                            <option value="<%= user.username %>"><%= user.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="mb-3">
                    <label for="password" class="form-label fw-bold">Senha:</label>
                    <input type="password" id="password" name="password" class="form-control" value="123456" required>
                </div>
                <% if (error) { %>
                    <p class="error"><%= error %></p>
                <% } %>
                <button type="submit" class="btn btn-primary">Entrar no Sistema</button>
            </form>
        </div>
    </div>
</body>
</html>